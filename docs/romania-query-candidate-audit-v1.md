# Romania Query Candidate Audit V1

The first narrow Romania local pilot tested `organizator portbagaj auto pliabil` and a narrower `organizator portbagaj auto pliabil 3 compartimente` formulation against current public marketplace surfaces.

Both fail the existing query-qualification gate. The visible eMAG surface mixes canonical foldable trunk organizers with cargo nets, seat-back tables, trash bins and other adjacent products, while the Trendyol surface mixes foldable organizers with hanging organizers, cargo nets, trunk covers and generic luggage/tool bags. The Trendyol 512+ total is a category-surface total, not a query-scoped comparable count.

Therefore the trunk-organizer pilot is recorded as `REJECTED_QUERY_SCOPE` and contributes no canonical Romania competition count. The next candidate is `set organizatoare valiza 6 piese`.

Policy: no verified-sales claim, no paid calls, no purchase authority.

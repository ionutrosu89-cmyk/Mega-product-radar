import {installCloudAutosync} from './cloud-sync.js';
await installCloudAutosync();
import {V6_STORAGE,allocateCapital,portfolioMetrics,feedbackCalibration,executiveActions} from './v6-core.js';
import {dedupePortfolio,upsertPortfolio,removePortfolio} from './portfolio-store.js';
const $=s=>document.querySelector(s),read=(k,f=[])=>{try{return JSON.parse(localStorage.getItem(k)||JSON.stringify(f));}catch{return f;}},write=(k,v)=>localStorage.setItem(k,JSON.stringify(v));
let radar=[],discovery=[];
function cleanExistingPortfolio(){const rows=read(V6_STORAGE.portfolio,[]),clean=dedupePortfolio(rows);if(JSON.stringify(rows)!==JSON.stringify(clean))write(V6_STORAGE.portfolio,clean);}
cleanExistingPortfolio();
async function load(){const [r,d]=await Promise.all([fetch('radar-live.json',{cache:'no-store'}).then(x=>x.json()).catch(()=>({products:[]})),fetch('discovery-live.json',{cache:'no-store'}).then(x=>x.json()).catch(()=>({products:[]}))]);radar=r.products||[];discovery=d.products||[];render();}
const itemList=(rows,key='name')=>rows.length?rows.map(x=>`<div class="item">${x[key]||'Produs'}</div>`).join(''):'<div class="item">Nimic acum</div>';
function candidateCapitalItems(){return radar.map(p=>({name:p.name,unitCost:Number(p.landed||0),profitPerUnit:Number(p.buyingDecision?.profit||p.profit||0),readiness:Number(p.buyingDecision?.readiness||p.buyingReadiness||0),risk:Number(p.buyingDecision?.killScore||p.killScore||0),score:Number(p.megaScore||p.score||0)}));}
function render(){const portfolio=dedupePortfolio(read(V6_STORAGE.portfolio,[])),feedback=read(V6_STORAGE.feedback,[]),pm=portfolioMetrics(portfolio),cal=feedbackCalibration(feedback),actions=executiveActions({radar,discovery,portfolio});$('#kCapital').textContent=`${pm.capitalBlocked.toFixed(0)} lei`;$('#kRevenue').textContent=`${pm.revenue30.toFixed(0)} lei`;$('#kGross').textContent=`${pm.grossProfit30.toFixed(0)} lei`;$('#kReorder').textContent=pm.reorder;$('#kStop').textContent=pm.stop;$('#kFeedback').textContent=cal.confidence;$('#buyList').innerHTML=itemList(actions.buy);$('#testList').innerHTML=itemList(actions.test);$('#reorderList').innerHTML=itemList(actions.reorder);$('#stopList').innerHTML=itemList(actions.stop);$('#feedbackSummary').textContent=`Sample ${cal.sample} • bias scor ${cal.scoreBias} • mediană marjă ${cal.marginMedian}% • penalizare retur ${cal.returnPenalty} • încredere ${cal.confidence}`;renderPortfolio(pm.actions);}
function renderPortfolio(rows){$('#portfolioTable').innerHTML=rows.length?`<table class="table"><thead><tr><th>Produs</th><th>Stoc</th><th>Vândute/30</th><th>Zile stoc</th><th>Acțiune</th><th></th></tr></thead><tbody>${rows.map(r=>`<tr><td>${r.name}</td><td>${r.stock}</td><td>${r.sold30}</td><td>${r.daysOfStock}</td><td class="${r.action==='REORDER'?'good':r.action==='STOP/REDUCE'?'bad':'warn'}">${r.action}</td><td><button data-delete-portfolio="${encodeURIComponent(r.name)}" type="button">Șterge</button></td></tr>`).join('')}</tbody></table>`:'<p class="note">Nu ai introdus încă produse în portofoliu.</p>';}
$('#allocate').onclick=()=>{const plan=allocateCapital(candidateCapitalItems(),Number($('#budget').value),{maxPerProductPct:Number($('#maxPct').value),minCashReservePct:Number($('#reservePct').value)});write(V6_STORAGE.capitalPlan,plan);$('#capitalSummary').textContent=`Deployed ${plan.deployed.toFixed(0)} lei • rezervă ${plan.reserve.toFixed(0)} lei • profit potențial ${plan.expectedProfit.toFixed(0)} lei`;$('#capitalPlan').innerHTML=plan.allocations.length?`<table class="table"><thead><tr><th>Produs</th><th>Qty</th><th>Capital</th><th>Profit pot.</th></tr></thead><tbody>${plan.allocations.map(x=>`<tr><td>${x.name}</td><td>${x.qty}</td><td>${x.capital.toFixed(0)}</td><td>${x.expectedProfit.toFixed(0)}</td></tr>`).join('')}</tbody></table>`:'<p class="note">Nu există produse eligibile cu date suficiente.</p>';};
$('#savePortfolio').onclick=()=>{const rows=read(V6_STORAGE.portfolio,[]),name=$('#pName').value.trim();if(!name)return;const item={name,stock:Number($('#pStock').value),sold30:Number($('#pSold').value),unitCost:Number($('#pCost').value),sellPrice:Number($('#pSell').value),returnsRate:Number($('#pReturns').value),updatedAt:new Date().toISOString()};write(V6_STORAGE.portfolio,upsertPortfolio(rows,item));render();};
$('#portfolioTable').addEventListener('click',e=>{const b=e.target.closest('[data-delete-portfolio]');if(!b)return;const name=decodeURIComponent(b.dataset.deletePortfolio);if(!confirm(`Ștergi ${name} din portofoliu?`))return;write(V6_STORAGE.portfolio,removePortfolio(read(V6_STORAGE.portfolio,[]),name));render();});
$('#saveFeedback').onclick=()=>{const rows=read(V6_STORAGE.feedback,[]);const name=$('#fName').value.trim();if(!name)return;rows.push({name,predictedScore:Number($('#fPred').value),actualMargin:Number($('#fMargin').value),returnRate:Number($('#fReturn').value),at:new Date().toISOString()});write(V6_STORAGE.feedback,rows);render();};
$('#refresh').onclick=load;load();

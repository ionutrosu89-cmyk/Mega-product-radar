import assert from 'node:assert/strict';import test from 'node:test';import {refreshViralScores} from '../viral-score-refresh.js';
test('refresh is held without explicit approval',async()=>{const x=await refreshViralScores({});assert.equal(x.status,'HELD');assert.equal(x.romaniaMissingAsScarcity,false);});
test('refresh calls guarded RPC and validates truth receipt',async()=>{let called=0;const fetchImpl=async()=>{called++;return {ok:true,json:async()=>({schema:'MPR_VIRAL_SCORE_REFRESH_RECEIPT_V1',scoresRefreshed:4,providerDataSpendEur:0,purchaseAuthorized:false,claimsSales:false,romaniaMissingAsScarcity:false})};};const x=await refreshViralScores({supabaseUrl:'https://example.supabase.co',serviceRoleKey:'secret',approved:true,fetchImpl});assert.equal(called,1);assert.equal(x.scoresRefreshed,4);});
test('optimistic Romania scarcity receipts are rejected',async()=>{const fetchImpl=async()=>({ok:true,json:async()=>({providerDataSpendEur:0,purchaseAuthorized:false,romaniaMissingAsScarcity:true})});await assert.rejects(()=>refreshViralScores({supabaseUrl:'x',serviceRoleKey:'x',approved:true,fetchImpl}),/POLICY_INVARIANT/);});

import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';
test('V2 owner review surface exposes required decision filters and conservative truth copy',()=>{const h=fs.readFileSync(new URL('../opportunity-review-v2.html',import.meta.url),'utf8'),j=fs.readFileSync(new URL('../opportunity-review-v2.js',import.meta.url),'utf8');for(const x of ['Top 100','Top 20','ROI minim','Confidence minim','IGNORE','WATCH','SHORTLIST','fără negociere'])assert.ok((h+j).includes(x));for(const x of ['marketplacePrice','supplierPublicPrice','conservativeLandedCost','matchConfidence','confidenceScore','profitPerUnitRon'])assert.ok(j.includes(x));assert.ok(j.includes('localStorage'));});
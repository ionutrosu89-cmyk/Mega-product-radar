import assert from 'node:assert/strict';import test from 'node:test';import {routeViralCandidatesToRomania} from '../viral-romania-router.js';
test('routing is held without explicit approval',async()=>{const x=await routeViralCandidatesToRomania({});assert.equal(x.status,'HELD');assert.equal(x.romaniaGapAssigned,false);});
test('routing validates truth-first receipt',async()=>{const fetchImpl=async()=>({ok:true,json:async()=>({schema:'MPR_VIRAL_ROMANIA_ROUTING_RECEIPT_V1',targetsRouted:3,providerDataSpendEur:0,purchaseAuthorized:false,romaniaGapAssigned:false})});const x=await routeViralCandidatesToRomania({supabaseUrl:'x',serviceRoleKey:'x',approved:true,fetchImpl});assert.equal(x.targetsRouted,3);});
test('automatic gap assignment is rejected',async()=>{const fetchImpl=async()=>({ok:true,json:async()=>({providerDataSpendEur:0,purchaseAuthorized:false,romaniaGapAssigned:true})});await assert.rejects(()=>routeViralCandidatesToRomania({supabaseUrl:'x',serviceRoleKey:'x',approved:true,fetchImpl}),/POLICY_INVARIANT/);});
